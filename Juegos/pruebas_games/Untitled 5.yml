info:
  name: Untitled 5
  type: http
  seq: 5

http:
  method: GET
  url: https://localhost:7020/api/Games/paginado?jugador=7
  params:
    - name: jugador
      value: "7"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
