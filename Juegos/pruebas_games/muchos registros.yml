info:
  name: muchos registros
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:7020/api/Games/seed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 300000
  followRedirects: true
  maxRedirects: 5
