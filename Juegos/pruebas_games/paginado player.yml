info:
  name: paginado player
  type: http
  seq: 11

http:
  method: GET
  url: https://localhost:7020/api/Games/paginado/player?game=1
  params:
    - name: game
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
