info:
  name: muchos registros players
  type: http
  seq: 7

http:
  method: POST
  url: https://localhost:7020/api/Games/seedPlayer
  auth: inherit

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
